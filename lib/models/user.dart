class User {
  final String uid;      //property that doesn't change as user navigates between screens
  User({this.uid});     //  set property in Constructor (named parameters)
}
//when new instance of User class created (pass UID property)